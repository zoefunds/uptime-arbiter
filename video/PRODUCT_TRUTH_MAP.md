# Product Truth Map — Uptime Arbiter

Every claim shown in the film must trace to a row here. Statuses: **working** / **working with limitations** / **simulated** / **mocked** / **planned** / **unclear**.

| Claim / feature | Status | Evidence | Safe wording | Visual treatment |
|---|---|---|---|---|
| Provider + customer pin evidence sources & exclusion terms at registration, before any dispute | working | `contracts/UptimeArbiter.py` `propose_sla()`; README trust-boundary section | "Pinned before any dispute exists — never after." | Real recording of the Register flow (`/register`) in the frontend |
| Dual-sided escrow + bond funding, activation | working | Verified live on current deployment per README Status section | "Both sides lock funds before the SLA goes active." | Real recording of registry/vault UI showing PROPOSED → ACTIVE |
| Claim submission snapshots window + evidence digest before evaluation | working | `submit_claim()` in contract; README | "The claim window is locked the moment it's filed." | Real recording of claims flow (`/claims/[claimId]`) |
| Independent multi-validator fetch, no claimant-supplied evidence trusted | working | `_run_breach_consensus`; README trust-boundary bullets | "Every validator fetches the sources itself — no one hands in their own evidence." | Animated architecture diagram (Remotion), not stock footage |
| Equivalence Principle compares computed breach-minutes, not raw text/booleans | working | README; `_run_breach_consensus` code comments | "Validators agree on a number, not on trusting each other's words." | Diagram: multiple validator nodes → converge on one number |
| Disagreement beyond tolerance → explicit INCONCLUSIVE (never silent default) | working | contract constants `CLAIM_STATUS_INCONCLUSIVE`, `_INCONCLUSIVE_SENTINEL`, comparison logic ~line 567-595 | "When the evidence doesn't agree, the contract says so — it never guesses." | State-machine visual: RESOLVED_* vs INCONCLUSIVE branch |
| Deterministic settlement math is separate from the nondeterministic LLM step | working | README; `_compute_settlement()` cited as distinct function | "The AI only measures downtime. A separate, deterministic function does the math." | Diagram: nondeterministic box → number → deterministic box |
| Additive-only challenges (can't replace/remove original pinned sources) | working | `file_challenge()`; README bullet | "A challenge can only add evidence — never swap it out." | Real recording of challenge UI, or diagram if UI not capturable in time |
| Full lifecycle resolved on current live deployment (propose → activate → claim → evaluate RESOLVED_NO_BREACH → challenge → re-adjudicate → bond slashed) | working | README "Verified on the current deployment" bullet, dated in MEMORY.md | "This exact sequence ran on the live deployed contract." | Screen-recorded evidence per stage; do NOT claim it was one continuous unbroken sitting |
| Finalize + pull-based withdrawal moves funds correctly | working (prior deployment) | README "Verified on a prior deployment" bullet | "Finalization and withdrawal have been verified — on a prior version of the deployed contract." | Vault/withdraw UI recording; caption must say "verified on a prior deployment" if using that footage |
| 39 passing direct-mode tests incl. proof EQ-principle re-derives (doesn't trust leader) | working | `tests/direct/`, README | "Independently of any live run, the test suite proves validators re-derive the answer themselves." | EvidenceCard-style UI showing test pass count, or terminal capture of test run |
| Finalize/withdraw currently blocked on live claim pending challenge window | working (design, not bug) | README explicit note | Only include if showing this state; caption "the contract enforcing its own timing rules" | Skip unless it strengthens the challenge-window beat |
| Backend never makes or influences breach determination | working | README: "backend is a pure read cache" | "The backend can't rule on a claim even if it wanted to — it only mirrors what's already on-chain." | Architecture diagram, backend shown as read-only branch |
| Single continuous live run of full lifecycle in one take | **does not exist / not claimed** | Challenge window = real wall-clock time (README) | Never imply one unbroken take; structure narration as "each stage, verified independently" | N/A — avoid this framing entirely |
| Mainnet deployment | **not applicable — StudioNet only** | README states StudioNet explicitly | Always say "GenLayer StudioNet," never "mainnet" or unqualified "GenLayer" in a way implying mainnet | End card must say StudioNet |
| Specific transaction hash / breach-minutes figure / payout amount shown on screen | unclear until recording | Depends what's captured live during filming | Only display numbers actually visible in a captured screenshot/recording; never invented | EvidenceCard / TransactionReceipt component sourced from real screen capture |
| Future roadmap items (more evidence types, mainnet, etc.) | planned / unconfirmed | Not stated by user or README | Omit entirely unless user supplies specific roadmap claims | N/A |

## Rule for implementation
Before any Remotion scene displays a number, status, or UI state, the STORYBOARD.md row for that scene must cite the exact source (recording timestamp, repo file+line, or README/MEMORY.md quote) it came from.
